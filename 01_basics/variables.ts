let greeting: string = "Hello Subash";
greeting.toUpperCase();

console.log(greeting);

// better code practice
let useId = 33445.122;
useId.toFixed(0);
console.log(useId);

let isLoggedIn: boolean = false;

export {};


let hero: string;

function printHero(){
          return "Iron Man";
}

hero = printHero();